import {
  Box,
  Card,
  CardBody,
  Text,
  Heading,
  Skeleton,
  Alert,
  Button,
  Badge,
  Divider,
} from "@razorpay/blade/components";

export const bladeScope: Record<string, unknown> = {
  Box,
  Card,
  CardBody,
  Text,
  Heading,
  Skeleton,
  Alert,
  Button,
  Badge,
  Divider,
};
